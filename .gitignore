node_modules/
screenshots/
scripts/current.pine
temp_*
*.log
discovery-log.json
rules.json
.ijfw/
.planning/
ijfw/
*.bak
.DS_Store

# Allow branding screenshots / capture guide (runtime tv screenshot output goes to /screenshots, which stays ignored)
!assets/screenshots/
!assets/screenshots/**

# Scratch buffers pulled off the live editor by scripts/pine_pull.js.
# These are whatever script happened to be open, which on this machine has
# meant private strategy sources landing in a PUBLIC repo. Never commit them.
scripts/current.pine
scripts/*.pine

# npm pack output
*.tgz
WHERE-THINGS-ARE.md

# Scratch probes. A killed cleanup step once let one of these reach a release
# commit via `git add -A`; tests/integration.test.js also fails on them.
/*-probe.mjs
/probe.mjs
/vis*.mjs
/front.mjs
